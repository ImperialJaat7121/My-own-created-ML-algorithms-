# Performance / Metrics

Summary
- Utility functions to compute regression and classification metrics used across tests.

Files
- `regression.py` — contains `calculate_mae`, `calculate_mse`, `calculate_rmse`, `calculate_r_squared`.

Quick run
- Import and call functions from `Own_ml_algorithms.performance.regression` in your scripts or tests.

Notes
- These are lightweight helpers intended for educational comparisons with scikit-learn metrics.
